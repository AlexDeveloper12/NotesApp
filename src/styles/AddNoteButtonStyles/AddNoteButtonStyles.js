import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    AddNoteButtonContainer: {
        marginTop: 20,
        justifyContent: 'center',
        alignItems: 'center'
    },
    btnBackgroundColour: {
        backgroundColor: '#5acc83'
    }

});

export default styles;