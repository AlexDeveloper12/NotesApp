import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    modalContainer: {
        height: '80%',
        backgroundColor: '#fff'
    },
    innerContainer: {
        flex: 1
    },
    input: {
        verticalAlign: 'top', 
        flex: 3
    },

});

export default styles;